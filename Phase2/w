#ifndef CLIENT_H
#define CLIENT_H

#include <iostream>
#include <fstream>
#include <string>
#include <memory>
#include <atomic>
#include <csignal>
#include <ctime>
#include <boost/asio.hpp>
#include <boost/beast.hpp>
#include <nlohmann/json.hpp>

void saveStatsToCSV(int boot_count, long uptime);
/**
 * @brief Saves system statistics to a CSV file.
 * @param boot_count The number of times the system has booted.
 * @param uptime The system uptime in seconds.
 */

void connectToServer(const std::string& host, int port, bool logToFile);
/**
 * @brief Connects to the WebSocket server and listens for periodic updates.
 * @param host The server's hostname or IP address.
 * @param port The port number to connect to.
 * @param logToFile If true, saves received stats to a CSV file.
 */

void signalHandler(int signum);
/**
 * @brief Handles SIGINT (Ctrl+C) signal to safely disconnect the client.
 * @param signum The signal number.
 */

#endif // CLIENT_H