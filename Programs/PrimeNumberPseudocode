//To check a number is prime or not

pseudofunctions:

function primeOrNot(number-->int)
 for (i = 2; i <= (number/2); i++)
    if number % i == 0
     return true
  end for
     return false
end function

function primeOrNotTest()
  input number
  result = primeOrNot(number)
  if (result)
     print "Number is prime"
  else
     print "Number is not prime"

pseudocode
   primeOrNotTest()

  
