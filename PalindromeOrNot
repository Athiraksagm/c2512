Pseudocode

function main
   string str
   string temp
   int i, j
   input str
   temp= str
   j=str. length()-1
  
while(i<j)
  swap(str[i], str[j])
  i++
  j--

if(temp==str) //palindrome
else          //not palindrome
  return 0
